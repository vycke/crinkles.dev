{% set _tags = tags | filterTags %}
<div class="list-group list-group--scale | cluster --gap-00">
  {% for tag in _tags %}
    <a href="/tags/{{ tag }}/" class="tag size-00" data-selected="{{ selected === tag }}">{{ tag }}</a>
  {% endfor %}
</div>