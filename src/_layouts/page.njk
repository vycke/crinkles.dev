---
layout: base
---

<section class="banner | contrast | center | mb-4">
  <div class="card | center maxw-{{ width or 5 }} flex --column --start">
    <span class="size-0 text-primary normal italic lowercase">&lt;{{ description }}&gt;</span>
    <h1 class="ml-1">{{ title }}</h1>
    <span class="size-0 text-primary normal italic lowercase">&lt;/{{ description }}&gt;</span>
  </div>
</section>


<main class="center --threshold-{{ width or 5 }} | mb-4">
  {{ content | safe }}
</main>
