---
layout: base
---

<section class="banner | contrast | center | mb-4">
  <div class="content | center maxw-{{ width or 5 }}">
    <span class="size-1 bold text-primary italic lowercase">&lt;{{ description }} /&gt;</span>
    <h1>{{ title }}</h1>
  </div>
</section>

<main class="center --threshold-{{ width or 5 }}">
  {{ content | safe }}
</main>

{% set older = pagination.href.next | objectify("url") %}
{% set newer = pagination.href.previous | objectify("url") %}
{% include 'components/pagination.njk' %}
